apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: bifrost
  namespace: ai-gateway
  labels:
    app: bifrost
    release: prometheus
spec:
  selector:
    matchLabels:
      app: bifrost
  endpoints:
  - port: http
    interval: 15s
    path: /metrics
    scheme: http
